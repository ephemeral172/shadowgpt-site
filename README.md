# shadowgpt-site

## Analytics (Google Analytics 4)

Copy `.env.example` to `.env` and set `VITE_GA_MEASUREMENT_ID` to your GA4 measurement ID (e.g. `G-XXXXXXXXXX`). Page views are tracked on every route change. Leave the variable unset to disable analytics (e.g. in development).
